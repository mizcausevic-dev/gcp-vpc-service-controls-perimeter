name: Deploy to vpcsc.kineticgain.com

# Static HTML -> Hostinger FTP at /vpcsc/
# workflow_dispatch-only: subdomain not yet in Hostinger DNS.

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-gcp-vpc-service-controls-perimeter
  cancel-in-progress: false

jobs:
  deploy:
    name: FTP-sync to vpcsc.kineticgain.com
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      - name: Verify build inputs
        run: |
          test -f index.html || (echo "FAIL: index.html missing" && exit 1)
          grep -q "GCP VPC Service Controls Perimeter" index.html || (echo "FAIL: index.html missing title" && exit 1)
          echo "OK: verified"

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./
          server-dir: /vpcsc/
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/node_modules/**
            README.md
            CHANGELOG.md
            LICENSE
            SECURITY.md
            CODE_OF_CONDUCT.md
            .gitignore
